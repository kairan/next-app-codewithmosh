"use client";

const ProductCard = () => {
  return (
    <div>
      <button className="btn btn-primary" onClick={() => console.log("test")}>
        add
      </button>
    </div>
  );
};

export default ProductCard;
